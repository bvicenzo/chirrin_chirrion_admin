<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="alert alert-<%= name == 'error' ? 'danger' : 'success' %>" role="alert">
      <%= content_tag :div, msg, :class => "flash_#{name}" %>
    </div>
  <% end %>
<% end %>
